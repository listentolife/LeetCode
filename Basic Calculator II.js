/**
 * @param {string} s
 * @return {number}
 */
var calculate = function(s) {
    let result = 0,
    	inter_res = 0,
    	num = 0;
    let op = '+',
    	ch;
    for (let i = 0; i < s.length; i++) {
    	if (s[i] === ' ') {continue;}
    	
    }
};